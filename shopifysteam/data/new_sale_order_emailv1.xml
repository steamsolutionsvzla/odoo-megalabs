<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="new_sale_order_emailv1" model="mail.template">
            <field name="name">Orden Simple</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="subject">Tu Orden {{ object.name }} ha sido confirmada</field>
            <field name="email_to">{{ object.partner_id.email }}</field>
            <field name="email_from">megalabs@steamsolutions.tech</field>
            <field name="body_html"><![CDATA[
<div>
    <p>Hola {{ object.partner_id.name }},</p>
    
    <p>Tu orden <strong>{{ object.name }}</strong> ha sido confirmada.</p>
    
    <p><strong>Total:</strong> {{ format_amount(object.amount_total, object.currency_id) }}</p>
    <p><strong>Fecha:</strong> {{ format_date(object.date_order) }}</p>
    
    <p>Accede a tu orden aquí: 
    <a href="{{ ctx.get('custom_link', '#') }}">Pagar</a></p>
    
    <p>Gracias,<br>
    El equipo de {{ object.company_id.name }}</p>
</div>
            ]]></field>
        </record>
    </data>
</odoo>